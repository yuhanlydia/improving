# Completed experiment part: sep20_ssd_5round_seed43_eval16_3090/ssd_round_1

Published only after a completed marker and evaluation metrics plus verified per-sample records exist. All verified rows are retained, including failed samples. Program text and token IDs are removed; correctness labels and program hashes support pass@k and AST richness recomputation. Original source hashes are recorded in publication_provenance.json. Program re-execution and token Jaccard recomputation require local originals.

JSONL details are gzip compressed. For files split into .chunkNNNN.gz, decompress chunks in numeric order and concatenate bytes to recover the whole compact file. No sample rows are dropped.
