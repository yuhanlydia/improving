# Use a separate virtual environment: the core experiment pins Transformers 4.51.3.
# Exact TRL version is enforced because we override its post-normalization hook.
torch==2.7.1
transformers==4.56.2
trl==0.23.1
peft==0.17.1
accelerate==1.10.1
datasets==4.0.0
numpy>=1.24,<3
PyYAML>=6,<7
safetensors>=0.4
