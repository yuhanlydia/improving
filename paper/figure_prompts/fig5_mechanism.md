# SPECTRUM — Figure 5: Future Mechanism and Ablation Evidence

**Production status: future experiment prompts only. No measured spectrum, attenuation, mechanism/outcome relationship, or ablation result is supplied. Do not render empirical curves, points, or heatmaps.**

## A. Evidence Inventory

### Principal scientific question

How does SPECTRUM's specified gradient second moment attenuation relate to held-out native-checkpoint accuracy and correct-code AST diversity, and how do confirmed τ, layer-selection, and rank-control ablations change that relationship?

### Source-supported answer

`CONCEPTUAL` and `DERIVED`: the operator defines a spectral gain law; it does not establish that the observed gradient spectrum has a particular shape, that spectral directions are algorithms, or that the operator causes a measured diversity improvement. `MISSING`: all empirical mechanism and ablation observations. Any association with held-out outcomes must remain a question until aligned data exist; it is not causal mediation evidence.

### Measurements and comparisons

| Status | Field | Meaning and required source |
|---|---|---|
| `MEASURED` | Mechanism results | None supplied for this future figure. |
| `CONCEPTUAL` | Gradient source | `g_{iu}` is a gradient for reference example i at nonpadding token position u at a native K/V projection output from completion-masked reference cross-entropy. Accumulate at **all nonpadding token positions**, including prompt positions with nonzero backpropagated gradients. Loss masking and second-moment position selection are different operations. |
| `DERIVED` | Matrix definition | `C = (1/M) Σ_{i,u} g_{iu}g_{iu}ᵀ` is the uncentered gradient second moment, where M counts the included example–position pairs; do not replace it with activation covariance, centered covariance, or weight SVD. |
| `DERIVED` | Normalization | `C̄ = C/λmax(C)`, `μ_i = λ_i(C)/λmax(C)`; per-module normalization, never a global normalization across heterogeneous modules. All empirical eigenvalues are `MISSING`. If λmax is zero or invalid, report invalid calibration; never divide silently. |
| `DERIVED` | Operator and gain | `T = [I + τ(I−C̄)]⁻¹`, default `τ=1`; eigen-direction gain `a_i(τ)=1/[1+τ(1−μ_i)]`. This identity is mathematical, not a measured attenuation curve. With nonnegative τ and valid C it attenuates lower normalized eigenvalue directions while the top normalized direction has gain one. |
| `CONCEPTUAL` | Intervention lifecycle | Temporarily fold `W′=TᵀW`, `b′=Tᵀb`; generate one raw completion per training prompt; restore original parameters; ordinary LoRA on all raw completions using all-token loss; merge native weights. Recompute C and T in each round. No filtering, hooks, prompt ensembles, or inference adapters. |
| `MISSING` | Spectrum fields | Native checkpoint/run ID, round, module/layer, K/V identity, calibration split and provenance, token count, matrix dimension, λ_i, sorted index, normalization denominator, numerical tolerance, and source artifact. |
| `MISSING` | Attenuation fields | Operator-derived gains and any independently measured output/projection attenuation, with exact measurement denominator, scope, and matched pre/post inputs. Do not conflate operator gains with measured gradient or activation changes. |
| `MISSING` | Held-out outcomes | Same configuration/run/seed IDs aligned to final native merged-checkpoint pass@1 (%) ↑ and D4AST ↑; confirmed dataset, task universe, verifier, draw budget, eligible tasks, and uncertainty. D4AST is expected normalized-AST coverage in four correct draws, an implementation proxy. |
| `MISSING` | Ablation manifest | Confirmed τ values, selected native K/V layers, Hard spectral rank values/retained-fraction definitions, optional low-rank approximation controls, compute accounting, and selection rule. Default SPECTRUM is the full-rank soft operator; rank controls do not redefine its default. |
| `CONCEPTUAL` | Comparison identity | `Plain self-distillation`, `SSD`, `Hard spectral`, `SPECTRUM` are candidate comparison labels; baseline details and which controls are actually run remain unconfirmed. Show baseline outcomes only if aligned data are supplied. |

### Statistics and uncertainty

Require `[MISSING: independent training seeds]`, `[MISSING: calibration replication and token sampling]`, `[MISSING: task/seed aggregation]`, `[MISSING: interval definition]`, `[MISSING: pairing keys]`, `[MISSING: tests and multiplicity plan]`, and `[MISSING: tie criterion]`. Eigen-directions and tokens are not independent training replicates. Per-module eigenvalues must not be pooled as identically indexed directions across different modules. Do not infer a CI for a deterministic operator identity. Measured attenuation requires its own sampling/aggregation definition. A scatter association, ablation, or spectral proxy is insufficient for a causal mediation claim.

### Derived quantities

`μ_i` and `a_i(τ)` use the formulas above. Optional outcome contrasts are `Δpass@1` in percentage points and `ΔD4AST` in expected distinct fingerprints relative to a named matched control. Optional scalar spectrum summaries require `[MISSING: preregistered summary definition]`; do not invent “spectral diversity,” fitted power laws, or an effective-rank definition after inspecting results. Per-task pass@1 is c/n. D4AST is `Σ_j[1−choose(c−n_j,4)/choose(c,4)]` if all correct completions have AST labels and c≥4, otherwise missing. Cross-method eligibility/cohort handling is unconfirmed.

### Missing information

There are no empirical arrays or coordinates to plot. Required raw/result tables must join on exact checkpoint, round, training seed, configuration, calibration provenance, and held-out evaluation IDs. Training loss uses all tokens; calibration CE is completion-masked. Layer and rank sweeps are proposals, not completed experiments. Dataset names, seed counts, τ-grid values beyond specified default 1, rank values, representative layers, and best operating points must remain `MISSING` until confirmed.

## B. Three Candidate Visualizations

| Option | Evidence narrative | Dominant panel | Supporting evidence | Data requirements | Main risk |
|---|---|---|---|---|---|
| 1 | Spectrum-to-behavior audit | Native K/V spectrum and gain field, 58% | Aligned held-out outcomes and ablation ledger, 42% | Module eigenvalues, gains, aligned outcomes, ablation manifest; empirical fields `MISSING` | Mathematical gain can be mistaken for measured mechanism. |
| 2 | Location and configuration audit | Layer × normalized-rank spectrum/gain matrix, 60% | Same-row outcomes and τ/layer/rank controls, 40% | Real module arrays, dimension alignment rule, same-run outcomes; `MISSING` | Decorative matrices can imply data or comparable eigenvectors. |
| 3 | Intervention-response audit | τ/layer/rank outcome sweep field, 60% | Exact spectra/gains for preregistered configurations, 40% | Confirmed sweep grids, held-out outcomes, spectra/gains and selection rule; `MISSING` | A sweep can imply a measured optimum or mediation. |

**Recommendation:** Option 1 after real module diagnostics and aligned held-out outcomes exist. It most directly distinguishes the specified transfer law from empirical spectra and behavior. All three are currently non-renderable specifications.

## C. Option 1 — Spectrum-to-Behavior Audit

### Design rationale

Emphasize which directions the operator would attenuate given the actual measured gradient second moment. Compress the ablation menu into a ledger and aligned outcomes. The primary risk is reading theoretical gain as empirical success; separate status labels are mandatory.

### Standalone generation prompt

Create planned Figure 5 for “SPECTRUM: Preserving Correct-Code Diversity in Self-Distillation,” a 178 × 112 mm ICLR-style double-column figure on white with 4 mm gutters. No empirical mechanism or ablation measurements are supplied. Ask how the specified gradient second moment gain law relates to held-out accuracy and correct-code AST diversity. Give panel (a), the spectrum/gain evidence field, 58% of usable area, panel (b), aligned native-checkpoint outcomes, 28%, and panel (c), the ablation/provenance ledger, 14%. The three-second relationship is the separation of `DERIVED: operator law` from `MISSING: empirical evidence`, not proof of a mechanism.

In (a), use vertically aligned subplots for actual normalized eigenvalues μ_i and gain a_i, faceted by `[MISSING: preregistered native K/V modules]` with identical sorted eigen-index axes. x is `Eigenvalue index (descending)`; y labels are `Normalized gradient second moment eigenvalue` and `Operator gain`. Require checkpoint, round, module, calibration token count, matrix dimension, λ_i, λmax, τ, and operator diagnostic fields. If dimensions differ, retain separate index ranges or use an explicitly defined i/d coordinate with the original d printed; never pretend indexed eigenvectors are shared across modules. No empirical curve may be drawn before actual arrays arrive; print `[MISSING: eigenvalues and gains]` and `[INSERT VERIFIED VALUE]` outside empty plotting areas. In an equation-only inset state `C = (1/M) Σ_{i,u} g_{iu}g_{iu}ᵀ`, `C̄ = C/λmax(C)`, `μ_i = λ_i(C)/λmax(C)`, `T = [I + τ(I−C̄)]⁻¹`, `a_i = 1/[1+τ(1−μ_i)]`, `Default τ = 1`. Label it `DERIVED: operator law`. It is not weight SVD and does not map eigen-directions to algorithms. Use linear y scales for normalized eigenvalues and gains unless a later empirically justified log scale is fully labeled; zero values may not be silently dropped.

In (b), show two aligned native-metric dot/interval facets labeled `Held-out pass@1 (%) ↑` and `Held-out D4AST ↑`. Each point requires the same run/configuration/seed IDs as panel (a), native merged checkpoint evaluation, verified task aggregation, interval metadata, and total/eligible tasks. Compare `Plain self-distillation`, `SSD`, `Hard spectral`, `SPECTRUM` only where those runs are supplied. Keep `[MISSING: aligned held-out outcomes]`, `[MISSING: aggregation and uncertainty]`, `[MISSING: independent seeds and task cohort]`, and `[MISSING: tests, correction, and tie rule]` as visible specification text. No seed, token, eigen-index, or completion is a substitute for independent training replication. D4AST is expected normalized-AST coverage among four correct draws; the common eligible cohort is `[MISSING]`.

Panel (c) lists `τ ablation`, `Layer ablation`, and `Rank control`, with `[MISSING: confirmed values and selection rule]`. Default SPECTRUM remains full-rank soft at τ=1; a rank threshold belongs to a specified Hard spectral or approximation control. Record `[MISSING: matched budget and calibration/evaluation provenance]`. Native K/V output gradients come from completion-masked reference CE accumulated at all nonpadding positions. Temporarily fold W′=TᵀW and b′=Tᵀb, generate one raw completion per training prompt, restore originals, train ordinary all-token LoRA on all raw outputs, merge natively, and recompute each round. No filters, hooks, prompt ensembles, or inference adapters are permitted in the method interpretation.

Use SPECTRUM blue #0072B2 diamond/solid line, Plain dark-gray circle, SSD medium-gray square, Hard spectral light-gray triangle/dashed line. τ/layer/rank variant labels are text and line patterns, never silently new method colors. Use 8 pt labels, 10 pt panel letters, sparse gray gridlines, 0–1 gain/normalized-eigenvalue axes, and no gradients. Exact text: `Figure 5 | Mechanism evidence — awaiting data`, `(a) Spectrum and specified gain`, `(b) Aligned held-out outcomes`, `(c) Ablations and provenance`, `DERIVED: operator law`, `MISSING: empirical evidence`, `Eigenvalue index (descending)`, `Normalized gradient second moment eigenvalue`, `Operator gain`, `Held-out pass@1 (%) ↑`, `Held-out D4AST ↑`, the four exact method names, and every placeholder above. Prohibit fabricated curves, eigenvectors-as-algorithms, weight spectra, significance, causal mediation, post hoc best-layer selection, fake attenuation measurements, unconfirmed sweep values, and dual axes. Preserve exactly all verified values, comparison sets, metric directions, uncertainty semantics, transformations, selection rules, and missing-data placeholders.

### Caption and statistical disclosure

**Figure 5 (planned; mechanism data missing).** The normalized spectrum of the uncentered native K/V gradient second moment and the analytically specified SPECTRUM gain are to be aligned with held-out native-checkpoint pass@1 (%) and D4AST, both higher-is-better. The operator identity is derived; spectra, outcomes, τ/layer/rank responses, and any measured attenuation are unavailable. Datasets, seeds, modules, aggregation, intervals, pairing, task eligibility, test/correction/tie rules, budgets, and selection rules are `[MISSING]`. No spectrum direction is an algorithm identity; no causal mediation or diversity improvement is established.

## D. Option 2 — Layer and Configuration Matrix

### Design rationale

Emphasize where spectral mass and attenuation occur across native K/V modules. Compress exact rank traces into two matrices while preserving per-module dimensions and provenance. The risk is apparent comparability of indexed directions; columns encode rank position only.

### Standalone generation prompt

Produce planned Figure 5 for “SPECTRUM: Preserving Correct-Code Diversity in Self-Distillation” as a 178 × 124 mm double-column white figure with 4 mm gutters. This is an unfilled future measurement design. The question is whether actual native K/V gradient second moment spectra and specified attenuation, across confirmed layers and controls, align with held-out accuracy and correct-code AST diversity. Make panel (a), a paired matrix field, 60% of usable area; panel (b), aligned held-out outcome strips, 25%; panel (c), τ/layer/rank configuration audit, 15%. The immediate message is a measurable location-by-rank diagnostic with empirical data missing, not an already discovered pattern.

Panel (a) contains two side-by-side real-data matrices only after arrays are supplied: `Normalized eigenvalues` and `Operator gains`. Rows are exact native module names in architecture order, grouped by configuration and seed; distinguish K and V in text. Columns are `Normalized eigenvalue index i/d` if module dimensions vary, with d recorded per row. Require `[MISSING: exact matrix binning or interpolation rule]`; do not resample a spectrum without a documented reduction, and do not treat equal ranks as the same eigenvectors. If no defensible common index is confirmed, use rows of small line traces within this dominant field. Cells must come from μ_i=λ_i(C)/λmax(C) or a_i=1/[1+τ(1−μ_i)], not synthetic texture. At present keep both matrices empty and show `[MISSING: module-level empirical arrays]`; unmeasured cells get neutral hatching, never a low-value color. Use distinct labeled sequential scales bounded 0–1; include numerical ticks, no spectrum averaging across modules by default, and no generated heatmap artwork. Place the equation text `C = (1/M) Σ_{i,u} g_{iu}g_{iu}ᵀ; C̄ = C/λmax(C); T = [I+τ(I−C̄)]⁻¹; Default τ = 1` in the margin under `DERIVED: operator law`. C is uncentered gradient second moment from completion-masked reference CE at all nonpadding native K/V output positions, not weight SVD or activation covariance.

Panel (b) contains run/configuration-aligned dot/interval strips labeled `Held-out pass@1 (%) ↑` and `Held-out D4AST ↑`, using raw native units. The data join is checkpoint, round, configuration, training seed, calibration provenance, and held-out evaluation ID. Require `[MISSING: aligned outcomes and matched budgets]`, `[MISSING: task/seed aggregation]`, `[MISSING: interval type and level]`, `[MISSING: independent seeds and pairing]`, `[MISSING: AST-eligible task cohort]`, and `[MISSING: test, correction, and tie rule]`. Do not infer uncertainty from variation among matrix columns. D4AST is expected normalized-AST coverage in four correct draws, not semantic algorithm count; report missing values when correct-sample labels are incomplete or c<4, and disclose cohort differences. Do not color matrix cells by downstream success or attach causal arrows.

Panel (c) lists the exact confirmed `τ ablation`, `Layer ablation`, and `Rank control` values with `[INSERT VERIFIED VALUE]` placeholders. Default SPECTRUM is full-rank soft at τ=1. A rank threshold is an explicitly named Hard spectral or approximation control, not an intrinsic soft-model truncation. Keep all preregistered configurations and failure/invalid-calibration rows; selecting representative modules requires `[MISSING: selection rule]`. Method lifecycle is temporary W′=TᵀW, b′=Tᵀb folding for one raw completion per training prompt, restoration, ordinary LoRA on all raw outputs with all-token loss, native merging, and fresh calibration each round; no filters, hooks, prompt ensembles, or inference adapters.

Use method identities SPECTRUM blue #0072B2 diamonds, Plain self-distillation dark-gray circles, SSD medium-gray squares, Hard spectral light-gray outlined triangles. Matrix magnitude uses a separate neutral sequential scale, clearly separated from method identity. Use 8 pt minimum text, 10 pt panel letters, horizontal module names, thin borders only around matrix extent, no cell numbers too small to read. Exact text: `Figure 5 | Layer diagnostics — awaiting data`, `(a) Native K/V spectrum and gain matrices`, `(b) Aligned held-out outcomes`, `(c) Configuration audit`, `Normalized eigenvalues`, `Operator gains`, `Normalized eigenvalue index i/d`, `DERIVED: operator law`, `Held-out pass@1 (%) ↑`, `Held-out D4AST ↑`, `τ ablation`, `Layer ablation`, `Rank control`, all four method names, and all missing placeholders above. Prohibit fabricated matrices, decorative heatmaps, eigenvectors-as-algorithms, weight SVD, invented measured attenuation, causal mediation, winner selection, significance, and arbitrary colormap normalization. Preserve exactly all verified values, comparison sets, metric directions, uncertainty semantics, transformations, selection rules, and missing-data placeholders.

### Caption and statistical disclosure

**Figure 5 (planned; matrices unmeasured).** Rows identify native K/V modules; rank-position columns do not identify common eigenvectors across modules. Normalized eigenvalues and analytically derived operator gains would be compared with run-aligned held-out pass@1 and D4AST and confirmed τ/layer/rank controls. Main finding: `[MISSING: empirical result]`. Matrix reduction, modules, configurations, seeds, datasets, task cohorts, budgets, aggregation, intervals, tests/correction/tie criteria, and selection rule are `[MISSING]`. The figure does not measure algorithms, establish causal mediation, or imply a completed ablation study.

## E. Option 3 — Intervention-Response Audit

### Design rationale

Emphasize the behavioral responses to explicit, controlled configuration changes, then inspect whether their spectral diagnostics are consistent with the proposed explanation. Compress module diagnostics into a few prespecified configurations. The risk is selecting favorable operating points after observing outcomes.

### Standalone generation prompt

Create planned Figure 5 for “SPECTRUM: Preserving Correct-Code Diversity in Self-Distillation,” 178 × 116 mm, double-column white scientific style with 4 mm gutters. No measurements from τ, layer, or rank ablations exist in the supplied evidence. Ask how controlled attenuation changes relate to native-checkpoint held-out accuracy and correct-code AST diversity. Reserve 60% of usable area for panel (a), one dominant intervention-response field, and 40% for panel (b), matched spectrum/gain diagnostics and a provenance footer. The three-second relationship is the separation of intervention settings, two native outcomes, and diagnostic evidence; it must not imply an optimum or completed mechanism test.

In (a), create three controlled-ablation bands: `τ ablation`, `Layer ablation`, and `Rank control`. Within each band show aligned pass@1 (%) and D4AST dot/interval subplots, never a dual y-axis. τ uses `[MISSING: confirmed τ grid]` on an ordered x axis; highlight τ=1 only as the specified default, not a best value. Layer selection is a categorical axis using `[MISSING: confirmed native K/V layer sets]`, with no connecting line implying continuous layer interpolation. Rank controls use `[MISSING: rank or retained-fraction definition and grid]`; Hard spectral truncation and any approximation control must be named distinctly from full-rank soft SPECTRUM. Require exact configuration, held-fixed hyperparameters, training seed, native checkpoint, matched budget, task universe, per-task metric, and interval fields. No line, point, or error bar may appear before verified values exist; retain `[MISSING: ablation observations]` and `[INSERT VERIFIED VALUE]`. Connect ordered settings only within a confirmed common configuration family. Include every planned configuration, failed run, and invalid calibration status, avoiding best-only selection.

In (b), use actual normalized-eigenvalue and gain traces for `[MISSING: preregistered representative configurations and modules]`, selected by a stated rule before outcome inspection. x is `Eigenvalue index (descending)` or a documented i/d normalization; y is `Normalized eigenvalue / gain` only when each trace uses the same 0–1 mathematical scale and is separately labeled. Prefer separate trace rows if this combined scale confuses evidence status. Empirical normalized eigenvalues μ_i=λ_i(C)/λmax(C) require λ_i and λmax(C); gain follows a_i=1/[1+τ(1−μ_i)] and is labeled `DERIVED: operator gain`. Any measured activation/output attenuation needs separate raw pre/post inputs and an exact ratio definition; until then say `[MISSING: independent attenuation measurement]`. C=(1/M)Σ_{i,u}g_{iu}g_{iu}ᵀ is the uncentered gradient second moment from completion-masked reference CE at every nonpadding native K/V output position. C̄=C/λmax(C), T=[I+τ(I−C̄)]⁻¹; τ=1 is the default. Do not introduce weight SVD, algorithms as eigenvectors, or a causal arrow from proxy to outcome.

The footer must retain `[MISSING: datasets, seeds, and pairing]`, `[MISSING: task/seed aggregation and uncertainty definition]`, `[MISSING: D4AST eligibility and common cohort]`, `[MISSING: matched compute and calibration budgets]`, and `[MISSING: tests, multiplicity correction, and tie rule]`. D4AST is expected normalized-AST coverage in four correct draws, an implementation proxy. Distinguish training-seed replication from tasks, tokens, eigenvalues, and completions. Model evaluation is native after temporary W′=TᵀW, b′=Tᵀb generation folding has been restored, one raw completion per training prompt has been used without filtering for ordinary all-token LoRA, and LoRA is merged; recompute the gradient second moment each round. No hooks, prompt ensembles, inference adapters, or train-output selection.

Use SPECTRUM blue #0072B2 diamonds/solid line, Plain self-distillation dark-gray circles, SSD medium-gray squares, and Hard spectral light-gray outlined triangles/dashed line whenever those aligned controls exist. Variant configuration labels use explicit text and line patterns. Set minimum type 8 pt, panel letters 10 pt, sparse grids, readable categorical labels, linear axes unless a verified sweep justifies a labeled log scale. Exact text: `Figure 5 | Intervention response — awaiting data`, `(a) Held-out response to controlled settings`, `(b) Matched spectral diagnostics`, `τ ablation`, `Layer ablation`, `Rank control`, `Default τ = 1`, `Held-out pass@1 (%) ↑`, `Held-out D4AST ↑`, `DERIVED: operator gain`, `Eigenvalue index (descending)`, the four method names, and all placeholders above. Prohibit invented sweep points, optimum labels, plausible error bars, inferred replication, weight spectra, eigenvectors-as-algorithms, causal mediation, post hoc module selection, fitted response laws, dual axes, and hidden adverse responses. Preserve exactly all verified values, comparison sets, metric directions, uncertainty semantics, transformations, selection rules, and missing-data placeholders.

### Caption and statistical disclosure

**Figure 5 (planned; ablation outcomes missing).** Confirmed τ, native K/V layer, and rank-control settings would be evaluated in held-out pass@1 (%) and D4AST, both higher-is-better, then aligned with actual normalized gradient spectra and the derived operator gain. Default τ=1 and the gain formula are method specification, not an observed optimum. Empirical result, configuration grids, datasets, seeds, control pairing, budgets, aggregation, intervals, eligibility, tests/correction/tie rules, and diagnostic selection are `[MISSING]`. Rank controls do not redefine full-rank soft SPECTRUM; diagnostic association does not establish causal mediation.

## F. Cross-option evidence-fidelity checklist

- Exactly three different evidence organizations use the same spectrum, gain, outcome, and ablation requirements.
- Every option has one dominant field of at least 50%; all supporting panels qualify the same mechanism question.
- Measured mechanism evidence is absent; no empirical spectra, matrices, gain measurements, outcomes, or ablation coordinates are fabricated.
- C is an uncentered native K/V output gradient second moment, computed over all nonpadding positions from completion-masked reference CE; it is not weight SVD.
- The derived gain law is visually and verbally separated from empirical eigenvalues and independent attenuation measurements.
- No eigenvector is labeled an algorithm; AST diversity remains an implementation proxy; no causal mediation is claimed.
- Default τ=1 and full-rank soft SPECTRUM are preserved; τ/layer/rank grids, selection rules, and tested control configurations remain unconfirmed.
- Data joins preserve checkpoint, round, seed, module, configuration, calibration, and held-out evaluation identity.
- Statistical units, task eligibility, uncertainty, pairing, tests, correction, and tie rules remain explicit missing requirements.
- All-token LoRA on all raw outputs, temporary folding/restoration, native merging, fresh per-round calibration, and the exclusion of filters/hooks/prompt ensembles/inference adapters remain exact.
